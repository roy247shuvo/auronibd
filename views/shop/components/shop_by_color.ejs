<div class="mb-16 px-4">
    <div class="text-center mb-8">
        <h2 class="font-bangla-title text-2xl text-[#800000] mb-1 flex items-center justify-center gap-2">
            <span class="animate-bounce">🎨</span> রঙের মেলা
        </h2>
        <p class="font-bangla-body text-xs text-gray-500">আপনার প্রিয় রঙের কালেকশন দেখুন</p>
    </div>

    <div class="flex flex-wrap justify-center gap-4">
        <% colors.forEach(c => { %>
            <a href="/shop?color=<%= c.name %>" class="group flex flex-col items-center gap-2 w-20">
                <div class="w-20 h-20 rounded-2xl overflow-hidden shadow-md border-2 border-white ring-1 ring-gray-100 relative group-hover:scale-105 transition-transform duration-300">
                    <% if (c.media_url && (c.media_url.endsWith('.mp4') || c.media_url.endsWith('.webm'))) { %>
                        <video src="<%= c.media_url %>" autoplay muted loop playsinline class="w-full h-full object-cover"></video>
                    <% } else { %>
                        <img src="<%= c.media_url || '/uploads/placeholder.jpg' %>" class="w-full h-full object-cover">
                    <% } %>
                    
                    <div class="absolute inset-0 opacity-20 mix-blend-overlay" style="background-color: <%= c.hex_code %>;"></div>
                </div>
                
                <span class="text-[11px] font-bangla-body font-bold text-gray-700 group-hover:text-[#800000]">
                    <%= c.name %>
                </span>
            </a>
        <% }) %>
    </div>
</div>